module.exports = function (ctx) {
    return {
        framework: {
            plugins: ['Loading'],
          
            config: {
              // optional (v0.17+)
              loading: {
                // Loading defaults
              }
            }
          }
    }
}